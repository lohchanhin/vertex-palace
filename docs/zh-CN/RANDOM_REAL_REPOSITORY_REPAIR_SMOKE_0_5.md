# 0.5 随机真实仓库修复冒烟测试

本研究仅使用一个随机目标，探索本地最新 Vertex Palace 0.5 候选版在真实仓库中的完整修复流程。它不是发布门槛，也不能单独证明修复正确率、路由质量、节省 Token 或提升速度。

## 冻结流程

1. 查询同时带有 `bug` 与 `good first issue` 标签的公开 GitHub issue。
2. 机械筛选活跃、公开、非 fork，且本机工具链可执行的 JavaScript、TypeScript 与 Python 仓库，并应用记录在证据中的规模限制。
3. 冻结完整候选池、候选池 SHA-256、256 位随机种子、唯一选中 issue、仓库 commit 和本地候选 CLI 哈希。
4. 以最小的 `SHA-256(seed + "\n" + issue URL)` 值决定唯一目标。
5. 在 Vertex Palace 看见选中任务前，先提交并推送选择证据。
6. 将冻结 commit 克隆到隔离的同级目录，记录仓库状态与基线行为。
7. 使用 `node dist/palace.cjs` 路由选中 issue，完成最小、可由证据支持的修复，并运行原仓库的聚焦验证。
8. 无论成功、产品失败、目标失效或环境失败，都保留原结果，不更换目标，也不向上游仓库推送。

## 解读边界

这次冒烟测试可以发现整合问题，并形成一个具体案例；单一随机 issue 不是 benchmark。无论结果好坏，都只能作为观察，不能包装成性能结论。
