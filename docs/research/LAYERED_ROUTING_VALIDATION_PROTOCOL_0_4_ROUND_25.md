# Layered Routing Validation Protocol (0.4.0-alpha.3, Round 25)

Round 25 is the second fresh qualification round for the unchanged `vertex-palace@0.4.0-alpha.3` candidate. Its complete 12-target manifest is `evidence/layered-routing-targets-round-25.json`. It is preregistered and hash-frozen before Round 24 results may alter any product, task, oracle, threshold, or runner input.

It uses the same public `vertex-palace@0.3.0` baseline, two repetitions per condition, balanced sequential order, four language families, four task strata, frozen GitHub cache protocol, layered truth definitions, and numerical gates as Round 24. All task identities and truth paths are new relative to Rounds 22-24.

Round 25 passes only when reference grounding and control abstention are 100%; routable core and declared auxiliary closure are 100%; macro core coverage is at least 0.90; macro focus is at least 0.70; every routable run satisfies 0.50 coverage and 0.40 focus; common-target coverage and focus are non-inferior within -0.05; repeated routes are deterministic; and there is no overconfidence, wrong forced stop, tracked-file pollution, metric disagreement, or context above 6,000 estimated tokens.

Its first observed result is immutable. Stable `0.4.0` and npm `latest` are authorized only if both Round 24 and Round 25 pass with the same candidate artifact integrity. A failure resets qualification and is preserved without target, oracle, or threshold edits.

This remains static-routing evidence. The separate 24-pair real Agent A/B is required for any Token, wall-time, or tool-call performance claim.
