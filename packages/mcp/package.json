{
  "name": "@vertex-palace/mcp",
  "version": "0.2.0",
  "description": "Vertex Palace adaptive context MCP stdio server.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "vertex-palace-mcp": "./dist/server.js"
  },
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "scripts": {
    "build": "tsup src/server.ts --format esm --dts --clean",
    "test": "vitest run --passWithNoTests",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@vertex-palace/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.9",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}
